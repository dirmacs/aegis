# ix memory-layer configuration (managed by aegis, deployed by docker-compose)
# Port: 8090 (native, no remap)
IX_MEMORY_URL=http://localhost:8090
ARANGODB_URL=http://localhost:8529

# ArangoDB credentials (change in production)
ARANGO_ROOT_PASSWORD={{ arango_root_password | default(value="changeme") }}
